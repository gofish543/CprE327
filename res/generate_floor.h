#ifndef GENERATE_FLOOR_H
#define GENERATE_FLOOR_H

#include <stdio.h>

#include "structs.h"

void generate_floor(GAME *game);

void generate_empty_text(GAME *game);

void generate_empty_map(GAME *game);

void generate_borders(GAME *game);

#endif
